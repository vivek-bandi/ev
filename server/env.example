# Environment Variables for EV Backend

# Database - Choose one option:

# Option 1: Local MongoDB (if installed locally)
MONGODB_URI=mongodb://localhost:27017/ev_vehicles

# Option 2: MongoDB Atlas (recommended for development)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ev_vehicles?retryWrites=true&w=majority

# Server
PORT=5000
NODE_ENV=development

# Client URL for CORS (add your Netlify domain here)
CLIENT_URL=https://vechileadmin.netlify.app

# JWT Secret (change this in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Cloudinary (for image uploads)
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# Admin credentials (for initial setup)
ADMIN_EMAIL=admin@ev.com
ADMIN_PASSWORD=admin123

# Production Configuration
# For Render deployment, set these environment variables:
# MONGODB_URI=your-mongodb-atlas-connection-string
# NODE_ENV=production
# CLIENT_URL=https://vechileadmin.netlify.app
# JWT_SECRET=your-production-jwt-secret
